@model IEnumerable<Upload_img_db.Models.ImageModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                        <a href="@Html.Encode(@item.ImageName)" target="_blank"> @Html.DisplayFor(modelItem => item.Title)</a>
                    
                    
                </td>

                
             <td>
                @Html.DisplayFor(modelItem => item.ImageName)
             </td>
             <td>
                @*<a asp-action="Edit" asp-route-id="@item.ImageId">Edit</a> |
                <aasp-action="Details" asp-route-id="@item.ImageId">Details</a> |*@
                  <a href="/ImageModels/DeleteConfirmed?id=@item.ImageId&blobName=@item.ImageName" onclick="return confirm('Are you sure?')"> Delete </a>
             </td>
            </tr>
}
    </tbody>
</table>
